# BuddyLine

Monorepo scaffold for **BuddyLine** (Vue 3 + Vite + TypeScript).

## Development

- Install dependencies:

```bash
pnpm install
```

- Run the web app:

```bash
pnpm dev
```

## Structure

- `apps/web/`: Vue app (Vite + TS)
